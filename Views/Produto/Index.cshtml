@model IEnumerable<Projeto_Final.Models.Produto>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<div class="container-criando">
    <p class="criando">
        <a asp-action="Create" class="criando">Create New</a>
    </p>
</div>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.MarcaCarro)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ModeloCarro)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CorCarro)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PrecoProduto)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Categoria)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Fornecedor)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MarcaCarro)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ModeloCarro)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CorCarro)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PrecoProduto)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Categoria.CategoriaId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Fornecedor.Id)
            </td>
            <td>
                    <a asp-action="Edit" asp-route-id="@item.ProdutoId" class="btn-editar">Editar</a> |
                    <a asp-action="Details" asp-route-id="@item.ProdutoId" class="btn-detalhes">Detalhes</a> |
                    <a asp-action="Delete" asp-route-id="@item.ProdutoId" class="btn-delete">Deletar</a>
            </td>
        </tr>
}
    </tbody>
</table>
